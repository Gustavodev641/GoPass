spring.application.name=demo

spring.datasource.url=${DB_URL:jdbc:postgresql://db-gopass-2.c1u2geuiqnoj.us-east-2.rds.amazonaws.com:5432/postgres}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:senha_aqui}

spring.jpa.hibernate.ddl-auto=update
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

aws.region=${AWS_REGION:us-east-2}
aws.bucket.name=${AWS_BUCKET:gopass-imagens}

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB